# Type Ndebe

Write Ndebe at [typendebe.com](https://typendebe.com/).

This repository contains the public website, script reference, fonts and keyboard assets.

Serve the directory with a static HTTP server for local development. Run checks with `node --test tests/*.test.cjs`.

Internal planning, research and review documents are maintained separately and must not be committed here.
